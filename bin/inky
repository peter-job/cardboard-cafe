#!/bin/bash

display_usage() { 
	echo "Setup inky phat."
    echo "This script must be run with super-user privileges."
	} 

if [[ ( $# == "--help") ||  $# == "-h" ]] 
then 
    display_usage
    exit 0
fi 

curl https://get.pimoroni.com/inky | bash